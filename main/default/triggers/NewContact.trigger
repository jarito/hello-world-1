trigger NewContact on Contact (after delete) {
	//before insert,before update
   // ContactTriggerHandler objContactTriggerHandler = new ContactTriggerHandler();
    //objContactTriggerHandler.CreateNewContact(Trigger.New, Trigger.OldMap);
    
}