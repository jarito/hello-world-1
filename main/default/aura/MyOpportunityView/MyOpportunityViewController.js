({
	handleOpportunityId : function(component, event, helper) {
		helper.handleOpportunityId(component, event, helper);
	},
	handleRecordUpdated : function(component, event, helper) {
		helper.handleRecordUpdated(component, event, helper);
	},
	editOpportunity : function(component, event, helper) {
		helper.editOpportunity(component, event, helper);
	},
	closeOpportunity : function(component, event, helper) {
		helper.closeOpportunity(component, event, helper);
	} 
})